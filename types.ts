export enum GameState {
  DASHBOARD,
  MODE_SELECTION,
  LEADERBOARD_SELECTION,
  LEADERBOARD,
  SETTINGS,
  LOADING_WORDS,
  LOADING_IMAGE,
  PLAYING,
  CORRECT_GUESS,
  GAME_OVER,
}

export enum GameMode {
  WORD_SPROUTS,
  MATH_ADDITION,
  COLOR_MATCHING,
}
